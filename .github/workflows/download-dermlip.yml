name: Download DermLIP Model to Release

on:
  workflow_dispatch: # اجازه اجرای دستی

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write # اجازه ساخت Release
    steps:
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install huggingface_hub
        run: pip install huggingface_hub

      - name: Download Model Files
        run: |
          python -c "
          from huggingface_hub import hf_hub_download
          import os, shutil

          model_id = 'redlessone/DermLIP_PanDerm-base-w-PubMed-256'
          

          os.makedirs('model_files', exist_ok=True)

           
              path = hf_hub_download(
                  repo_id=model_id,
                  filename=filename,
                  local_dir='./model_files',
                  local_dir_use_symlinks=False
              )
              print(f'Downloaded to {path}')
          "

      - name: Create Release and Upload Files
        uses: softprops/action-gh-release@v2
        with:
          tag_name: dermlip-v1.0
          name: "DermLIP PanDerm Model v1.0"
          body: "A dermatology vision-language model (CLIP-style). Contains safetensors and PyTorch files."
          files: |
            model_files/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
